# 乒乓智配首轮实现审计

本文只把当前仓库能够复核的结果写成已完成；主机测试、OpenSCAD 编译和 PCB 包络检查不替代实物、电气波形或生产释放。旧版离散光栅、网顶轨道、圆柱卡网件和独立外挂托架只保留在历史文件中，不属于当前主线。

## 当前主线结论

| 范围 | 当前可复核结果 | 仍需实物/生产证据 |
| --- | --- | --- |
| 机械源与装配 | [`hardware/cad/net_stand.scad`](../hardware/cad/net_stand.scad) 已包含完整球台截面、左右夹体/立柱、无网顶轨道的网布路径、PVDF 端部安装位、20 个 M6 外购件包络、M6 分体壳、球头、电子腔、y+ 一体式 UI 填平固定板和线缆导向。 | 网布真实厚度/张力、M6 SKU 实物尺寸、球头螺纹、线缆弯曲半径和装配对准。 |
| 夹体分体 | 左右 C 形夹按 y=0 分型为两半，斜立柱和绿色载体再在顶部下 `30 mm` 分成下段/上段；下段底面与 C 夹黄灰交界 `z=16 mm` 共面，上段延伸到 `z=260.5 mm`，不向下插入 C 形座；网布/圆柱功能区止于 `z=168.5 mm`。取消脚、裤裆、滑轨和外套圈；必要的网布过道和侧开接收腔仍保持功能开口。 | 实际打印间隙、共面落座插入力、反复拆装耐久和真实夹紧/网布张力下的变形。 |
| 立柱分件落座 | `post_clamp_carrier_lower` 承载绿色底座、网布过道和 Ø10 圆柱插杆侧开接收腔，`post_clamp_carrier_upper` 是顶部 `30 mm` 可替换段；两件 C 夹半体沿 y=0 分型面合拢，主板由两端 `[`/`]` 夹件承托。下段底面与 C 夹最高承托面共面，上段带公母键、四个 `Ø3.4 mm` 通孔和顶部 M8 支撑孔，四枚普通盘头/低矮圆头 M3×40 进入下段 `Ø2.4×10 mm` 导向盲孔；网布/圆柱功能区为 `z=16…168.5 mm`。 | 打印收缩、共面落座插入力、振动/倾覆/网布张力测试。 |
| 网布安装 | 网顶不设置轨道。网布从球台中心侧穿过两侧立柱的连续 `3 mm` 过道，端部止到立柱外表面；空心边套套到 Ø10 圆柱插杆后沿 x 推入下段侧开接收腔，立柱在距顶部 `30 mm` 处分成上下两段，由公母键和四枚普通盘头/低矮圆头 `M3×40` 连接；上段安装后封住入口。 | 网布白边实际厚度、张力、侧开接收腔防脱行程与释放手感、插杆推入力、螺钉啮合和长期疲劳。 |
| UI 面板与 M6 盖件 | 电子腔使用 y+ 一体式 UI 填平固定板；M6 前/后/底盖和线缆压紧件独立验证；目标是装后齐平、无明显错台或松动。 | 实物面板收缩、8 枚自攻钉固定强度、M6 盖件贴合和现场塞尺/纸条检查；不宣称 IP 等级。 |
| 打印包 | 导出脚本当前生成 `41` 个正式 STL：`37` 个 PETG、`4` 个 TPU/柔性。256 mm 拼盘为 `7` 张板、`41` 件已排版、`0` 件超尺寸；两件下段载体采用 `rx=0°、ry=51°、rz=45°` 三轴斜放，上段按分型面平放，并按当前排版清单复核。 | 切片器支撑/首层、G-code、打印机实际成形、材料和实物装配。 |
| ESP32/电子机械 | 已有 4 个原生 KiCad PCB/项目、4 个原理图 PDF、Atopile 接口合同、板级 STL/STEP、主控/接收/发射电源/UI 分板、左右内置电池、端子化线束、按钮/屏幕/指示灯/蜂鸣器/扬声器/USB-C 预留和统一 fit report。 | 铜箔布线、最终器件料号、ERC/DRC 生产门、升压峰值/温升/纹波、真实电池/连接器/波形和上电。 |
| 功能/协议 | M6 十路逻辑映射、SPI/IRQ/FIFO、PVDF 波形窗口、事件状态、断链缓存和 Atopile/主机回归链已有代码证据。 | 20 枚 M6 实物逐通道输出、NPN 极性/电平、光耦波形、左右光轴、真实球事件和 App/设备联调。 |

## 当前验证入口

```text
python3 hardware/cad/validate_net_stand.py
python3 hardware/cad/export_net_stand_printables.py --clean
python3 hardware/cad/build_print_platter.py --preset x1c-256 --clean
python3 hardware/cad/test_preview_consistency.py
python3 hardware/electronics/validate_system_fit.py
```

`validate_net_stand.py` 负责 OpenSCAD 参数探针、左右镜像、当前打印件编译/包络、分段斜立柱与 C 夹接触/装配合同、网布过道/Ø10 圆柱插杆侧开接收腔和四枚立柱分型螺钉合同、桌下免打孔路径以及组合诊断件。`validate_system_fit.py` 负责原生 KiCad 板框/安装孔/3D 模型、电子腔 AABB/布尔干涉、M6 载板竖放包络、Atopile/原理图和当前 `41` 件打印包摘要。

## 证据边界

- “KiCad 文件存在”证明板框、器件占位和接口可审查，不证明铜箔已经完成或可直接下 Gerber 生产单。
- “Atopile validate/build 通过”证明接口合同可解析，不替代原理图 ERC、PCB DRC、上电和波形。
- “STL watertight、拼盘 0 超尺寸”证明封闭几何和矩形排版，不证明首层、切片、打印强度或实际安装。
- “盒盖水密”在本项目中只表示盖合后无明显开缝/错台/松动，不是防水等级认证。

现场放行统一使用 [`validation-matrix.zh-CN.md`](validation-matrix.zh-CN.md) 和 [`field-validation-record-template.zh-CN.md`](field-validation-record-template.zh-CN.md)。没有照片、量具、示波器/逻辑分析仪或真实事件日志时，状态保持 `pending`。
