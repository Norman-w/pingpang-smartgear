# 乒乓智配当前支架可视核验记录 v0.1

本文记录当前 `hardware/cad/net_stand.scad` 的人工可视核验范围和结论。它证明的是 OpenSCAD 装配关系在渲染图中可读，不替代 PETG 实物强度、标准件装配、光学对准或球台现场验收。

## 可复现入口

```text
python3 hardware/cad/render_net_stand_preview.py \
  --output-dir /tmp/pingpang-smartgear-render
python3 hardware/cad/render_m6_views.py \
  --part m6_detector_mount \
  --output-dir /tmp/pingpang-smartgear-render/m6-audit
python3 hardware/cad/render_m6_views.py \
  --part m6_detector_exploded \
  --output-dir /tmp/pingpang-smartgear-render/m6-audit
python3 hardware/cad/export_m6_component_previews.py \
  --clean \
  --output-dir /tmp/pingpang-smartgear-render/m6-component-previews
```

CI 也会在 `OpenSCAD geometry validation` job 中生成并上传当前支架和历史外挂 X 夹具的 PNG artifact。当前支架的渲染脚本输出包括整体装配、左右支架、桌下夹持剖面、下段立柱+夹体一体件、立柱分件、网顶拼接、M6 检测器总成/无剖切爆炸图、M6 各单件、PVDF 座；专用 `render_m6_views.py` 还为总成和爆炸图各输出 x-/x+/y-/y+/z-/z+ 六视图及等轴视图。旧 STG-120ML 托架和标定规仍单独渲染用于历史回归，不作为当前采购件结论。

## 本轮检查项

| 检查对象 | 可视核验内容 | CAD 结论 | 实体验收边界 |
| --- | --- | --- | --- |
| 整体装配 | 左右立柱、真实网布占位、网顶承载条、左右各十路 M6 直角对射器件、10×56×216 mm 铝合金主体、一体 T 尾座、前后盖、底盖、金属 90°支撑、竖直采购球头、PVDF 座和球台截面 | 关系可读；左右各 10 枚器件按 20 mm 单列节距装入主体，左侧发射光轴沿 +x、右侧接收端面朝 −x；球头经 T 尾座和金属支撑连接到网夹 | 网布张力、立柱平行度、网顶实测高度、M6 器件实际光学中心、线缆净空和 NPN 输出仍待实物 |
| 桌下夹持剖面 | 上夹板跨台边、上表面可替换保护垫、台底独立压块、圆头 M8 螺杆、固定下臂螺母、旋钮内两枚对锁螺母和前后两片三角侧肋 | 上保护垫位于桌面与固定上夹板之间；螺杆和台底压块均位于台面底面以下，不需要桌面孔；三角侧肋与上夹板/外墙/下臂形成重叠抗倾覆力路；旋钮对锁叠层明确承担螺杆驱动；当前 CAD 外轮廓相对台边名义外伸 `160 mm`，满足项目下限 `130 mm` 并收敛到 ITTF T2 水平部分上限 | 夹紧力、滑移、保护垫压缩/压痕、螺杆行程、对锁防松、三角肋强度和标准件实际装配仍待实物 |
| 立柱分件 | 下段+固定 C 形夹一体件、上段、外套筒和内芯 | 首样下段不再与夹体相互穿入；接缝分件和插接方向可读；没有把 `post` 装配预览当成单件打印 | 插接间隙、抗扭、层间强度和倾斜仍待实物 |
| 网顶承载条 | 三段搭接、拼接片孔位、两侧承托/端挡 | 全跨度承载关系和接缝锁紧位置可读 | 网布白边固定、张力和长期变形仍待实物 |
| M6 光电阵列、壳体与承力路径 | 左右各一根 10×56×216 mm 长条主体，y− 一体 T 尾座，x− 正球弧前盖、x+ 圆角矩形后盖、底盖、10 个单列通道、M8 球头和金属 90°支撑 | 主体包络、前后盖分型、T 尾座直接攻 M8 内丝、后盖让位、底盖开放线缆孔和完整承力链可读；没有把 PETG 壳体当成球头承力件 | 实物螺纹/螺母、光学中心、发射/接收方向、线缆弯曲、环境光、邻道串扰和逐通道对准仍待接板 |
| PVDF 座 | 座体、可拆薄膜、两侧压片和网顶承载条的相对位置 | 薄膜没有被永久并入打印本体，压片关系可读 | 贴合预紧、AFE 波形、机械误报和阈值仍待接板 |
| 高度通道与历史标定规 | 当前 M6 `+10…+190 mm`、`20 mm` 节距的十路单列通道；另保留 STG-120ML 3.87 mm 标定规历史渲染 | 当前只验证通道高度/装配关系；历史标定规不作为 M6 精度依据，也不宣称 NPN 已经接入主控 | 真实光轴高度、球体遮挡宽度、读数误差、NPN 电平转换和校准版本仍待实物 |

## 结论边界

- 当前 CAD 的装配方向、镜像关系、免打孔夹持路径、M6 x 轴光学方向、20 mm 十路主体、前后盖分体和一体 T 尾座承力路径已通过可视核验及自动几何回归。
- M6 总成爆炸图使用完整实体，不使用剖切；“前盖更大且遮住激光头/单枚螺帽”的结论只针对当前 CAD 包络，仍须按真实 SKU 首件复核。
- M8 内丝在 STL 中以 `Ø6.8 mm` 攻牙底孔和入口 `Ø9.5×1 mm` 沉台表示，实际 `M8×1.25` 螺旋牙由加工方攻制；主体、支撑和网夹适配板输出为机加工/外购预览，前后盖和底盖才是 PETG 候选打印件。
- 独立组件导出包包含左右各一套主体、前盖、后盖、底盖、90°支撑和网夹适配板，共 12 个完整 STL；M6 总成和无剖切爆炸图各有 7 张视图（六视图 + 等轴），方向箭头为审计叠加，不进入零件 STL。
- 渲染图不能证明支架在目标球台上夹得住，也不能证明 PVDF 或红外光栅已经有有效电气信号。
- `docs/field-validation-record.example.json` 中的 M/B/S/T/E 现场项目继续保持 `pending`，在没有照片、量具/示波器记录、原始波形和真实事件日志前不改为 `pass`。
